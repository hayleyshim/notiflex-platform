# 테넌트 공통 base. namespace를 지정하지 않으므로,
# ArgoCD가 각 Application의 destination.namespace(tenant-<이름>)로 배포한다.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - valkey.yaml
  - notiflex.yaml
  - quota.yaml
  - networkpolicy.yaml
